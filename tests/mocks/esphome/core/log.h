// Minimal ESPHome log mock — all log macros are no-ops
#pragma once

#define ESP_LOGE(tag, ...)
#define ESP_LOGW(tag, ...)
#define ESP_LOGI(tag, ...)
#define ESP_LOGD(tag, ...)
#define ESP_LOGV(tag, ...)
#define ESP_LOGCONFIG(tag, ...)
